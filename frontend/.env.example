# Copy to .env.local and fill in once the services are deployed.
# Not used yet — pages currently read from src/mockApi.js.

VITE_BACKEND_URL=http://localhost:8000
# Not called directly by the frontend (per docs/api-contracts.md — price
# forecasts and routes come back through the backend), kept here for parity
# with the other services' env files.
VITE_AI_SERVICE_URL=http://localhost:8001
